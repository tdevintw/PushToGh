#include <stdio.h>
#include <stdlib.h>

int main()
{
int number;
printf("enter the dicimal number:");
scanf("%d",&number);
printf("the number in octal form is %o\n",number);
printf("the number in héxadecimal form is %x\n",number);
return 0;
}
